version: "3.8"

services:
  app:
    image: blog
    container_name: blog-container
    build:
      context: .
    volumes:
      - ./server/prisma:/backend/prisma
      - ./server/uploads:/backend/uploads
    ports:
      - "4000:4000"
    deploy:
      resources:
        limits:
          memory: 120M
    restart: on-failure
